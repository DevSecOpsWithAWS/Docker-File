apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense-updated
  region: us-east-1

managedNodeGroups:
  - name: expense-updated
    instanceTypes: ["m5.large", "c3.large","c4.large","c5.large"]
    desiredCapacity: 3
    spot: true